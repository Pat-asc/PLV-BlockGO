---
# Fabric CA for Registrar Organization
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fabric-ca-registrar-pvc
  namespace: plv-main-campus
spec:
  accessModes: [ "ReadWriteOnce" ]
  storageClassName: fabric-storage
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fabric-ca-registrar
  namespace: plv-main-campus
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fabric-ca-registrar
  template:
    metadata:
      labels:
        app: fabric-ca-registrar
        org: registrar
    spec:
      serviceAccountName: fabric-sa
      nodeSelector:
        topology.kubernetes.io/zone: "__GKE_ZONE_A__"
      initContainers:
      - name: init-ca
        image: alpine:latest
        imagePullPolicy: IfNotPresent
        command: ["sh", "-c", "mkdir -p /data/msp/keystore && (cp -L /raw/ca-cert.pem /data/ca-cert.pem 2>/dev/null || true) && (cp -L /raw/tls-cert.pem /data/tls-cert.pem 2>/dev/null || true) && (cp -L /raw/ca-key.pem /data/msp/keystore/key_sk 2>/dev/null || true) && (cp -L /raw/tls-key.pem /data/msp/keystore/tls_key_sk 2>/dev/null || true) && chmod -R 700 /data"]
        volumeMounts:
        - name: raw-identity
          mountPath: /raw
        - name: ca-data
          mountPath: /data
      containers:
      - name: fabric-ca
        image: hyperledger/fabric-ca:1.5.7
        command: ["sh", "-c", "fabric-ca-server start -b admin:${BOOTSTRAP_PASS} --ca.certfile /etc/hyperledger/fabric-ca-server/ca-cert.pem --ca.keyfile /etc/hyperledger/fabric-ca-server/msp/keystore/key_sk --tls.certfile /etc/hyperledger/fabric-ca-server/tls-cert.pem --tls.keyfile /etc/hyperledger/fabric-ca-server/msp/keystore/tls_key_sk -d"]
        ports:
        - containerPort: 7054
          name: ca-port
        env:
        - name: FABRIC_CA_HOME
          value: /etc/hyperledger/fabric-ca-server
        - name: FABRIC_CA_SERVER_CA_NAME
          value: ca-registrar
        - name: FABRIC_CA_SERVER_TLS_ENABLED
          value: "true"
        - name: BOOTSTRAP_PASS
          valueFrom:
            secretKeyRef:
              name: blockgo-secrets
              key: FABRIC_CA_REGISTRAR_PASS
        volumeMounts:
        - name: ca-data
          mountPath: /etc/hyperledger/fabric-ca-server
      volumes:
      - name: ca-data
        persistentVolumeClaim:
          claimName: fabric-ca-registrar-pvc
      - name: raw-identity
        secret:
          secretName: ca-registrar-identity
---
apiVersion: v1
kind: Service
metadata:
  name: ca-registrar
  namespace: plv-main-campus
spec:
  selector:
    app: fabric-ca-registrar
  ports:
  - port: 7054
    name: ca
---
# Fabric CA for Faculty Organization
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fabric-ca-faculty-pvc
  namespace: plv-annex-campus
spec:
  accessModes: [ "ReadWriteOnce" ]
  storageClassName: fabric-storage
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fabric-ca-faculty
  namespace: plv-annex-campus
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fabric-ca-faculty
  template:
    metadata:
      labels:
        app: fabric-ca-faculty
        org: faculty
    spec:
      serviceAccountName: fabric-sa
      nodeSelector:
        topology.kubernetes.io/zone: "__GKE_ZONE_B__"
      initContainers:
      - name: init-ca
        image: alpine:latest
        imagePullPolicy: IfNotPresent
        command: ["sh", "-c", "mkdir -p /data/msp/keystore && (cp -L /raw/ca-cert.pem /data/ca-cert.pem 2>/dev/null || true) && (cp -L /raw/tls-cert.pem /data/tls-cert.pem 2>/dev/null || true) && (cp -L /raw/ca-key.pem /data/msp/keystore/key_sk 2>/dev/null || true) && (cp -L /raw/tls-key.pem /data/msp/keystore/tls_key_sk 2>/dev/null || true) && chmod -R 700 /data"]
        volumeMounts:
        - name: raw-identity
          mountPath: /raw
        - name: ca-data
          mountPath: /data
      containers:
      - name: fabric-ca
        image: hyperledger/fabric-ca:1.5.7
        command: ["sh", "-c", "fabric-ca-server start -b admin:${BOOTSTRAP_PASS} --ca.certfile /etc/hyperledger/fabric-ca-server/ca-cert.pem --ca.keyfile /etc/hyperledger/fabric-ca-server/msp/keystore/key_sk --tls.certfile /etc/hyperledger/fabric-ca-server/tls-cert.pem --tls.keyfile /etc/hyperledger/fabric-ca-server/msp/keystore/tls_key_sk -d"]
        ports:
        - containerPort: 7054
        env:
        - name: FABRIC_CA_HOME
          value: /etc/hyperledger/fabric-ca-server
        - name: FABRIC_CA_SERVER_CA_NAME
          value: ca-faculty
        - name: FABRIC_CA_SERVER_TLS_ENABLED
          value: "true"
        - name: BOOTSTRAP_PASS
          valueFrom:
            secretKeyRef:
              name: blockgo-secrets
              key: FABRIC_CA_FACULTY_PASS
        volumeMounts:
        - name: ca-data
          mountPath: /etc/hyperledger/fabric-ca-server
      volumes:
      - name: ca-data
        persistentVolumeClaim:
          claimName: fabric-ca-faculty-pvc
      - name: raw-identity
        secret:
          secretName: ca-faculty-identity
---
apiVersion: v1
kind: Service
metadata:
  name: ca-faculty
  namespace: plv-annex-campus
spec:
  selector:
    app: fabric-ca-faculty
  ports:
  - port: 7054
    name: ca
---
# Fabric CA for Department Organization
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fabric-ca-department-pvc
  namespace: plv-pubad-campus
spec:
  accessModes: [ "ReadWriteOnce" ]
  storageClassName: fabric-storage
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fabric-ca-department
  namespace: plv-pubad-campus
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fabric-ca-department
  template:
    metadata:
      labels:
        app: fabric-ca-department
        org: department
    spec:
      serviceAccountName: fabric-sa
      nodeSelector:
        topology.kubernetes.io/zone: "__GKE_ZONE_C__"
      initContainers:
      - name: init-ca
        image: alpine:latest
        imagePullPolicy: IfNotPresent
        command: ["sh", "-c", "mkdir -p /data/msp/keystore && (cp -L /raw/ca-cert.pem /data/ca-cert.pem 2>/dev/null || true) && (cp -L /raw/tls-cert.pem /data/tls-cert.pem 2>/dev/null || true) && (cp -L /raw/ca-key.pem /data/msp/keystore/key_sk 2>/dev/null || true) && (cp -L /raw/tls-key.pem /data/msp/keystore/tls_key_sk 2>/dev/null || true) && chmod -R 700 /data"]
        volumeMounts:
        - name: raw-identity
          mountPath: /raw
        - name: ca-data
          mountPath: /data
      containers:
      - name: fabric-ca
        image: hyperledger/fabric-ca:1.5.7
        command: ["sh", "-c", "fabric-ca-server start -b admin:${BOOTSTRAP_PASS} --ca.certfile /etc/hyperledger/fabric-ca-server/ca-cert.pem --ca.keyfile /etc/hyperledger/fabric-ca-server/msp/keystore/key_sk --tls.certfile /etc/hyperledger/fabric-ca-server/tls-cert.pem --tls.keyfile /etc/hyperledger/fabric-ca-server/msp/keystore/tls_key_sk -d"]
        ports:
        - containerPort: 7054
        env:
        - name: FABRIC_CA_HOME
          value: /etc/hyperledger/fabric-ca-server
        - name: FABRIC_CA_SERVER_CA_NAME
          value: ca-department
        - name: FABRIC_CA_SERVER_TLS_ENABLED
          value: "true"
        - name: BOOTSTRAP_PASS
          valueFrom:
            secretKeyRef:
              name: blockgo-secrets
              key: FABRIC_CA_DEPARTMENT_PASS
        volumeMounts:
        - name: ca-data
          mountPath: /etc/hyperledger/fabric-ca-server
      volumes:
      - name: ca-data
        persistentVolumeClaim:
          claimName: fabric-ca-department-pvc
      - name: raw-identity
        secret:
          secretName: ca-department-identity
---
apiVersion: v1
kind: Service
metadata:
  name: ca-department
  namespace: plv-pubad-campus
spec:
  selector:
    app: fabric-ca-department
  ports:
  - port: 7054
    name: ca
